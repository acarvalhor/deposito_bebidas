server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/controle_bebidas?useTimezone=true&serverTimezone=UTC
    username: root
    password: root123
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL5Dialect